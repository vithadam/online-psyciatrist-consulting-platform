<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Signup Form</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Gruppo&display=swap" rel="stylesheet">
    <!-- CSS Styles -->
    <style>
        /* Global styles */
        * {
            color: #fff; 
          }

        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(to bottom, navy, #b24eff); /* Gradient background */
            height: 100vh; /* Full height of the viewport */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Container for the signup form */
        .signup-container {
            background-color: transparent;
            border: 1px solid #fff;
            padding: 20px;
            text-align: center;
            width: 600px;
        }

        /* Header style */
        .signup-container h2 {
            text-align: center;
            background-color: transparent;
            color: #fff;
        }


        #age{width: 100px;

        }

        /* Input group styles */
        .input-group {
            margin-bottom: 15px;
            display: flex; /* Flexbox layout for input elements */
        }

        /* Input and select styles */
        .input-group input,
        .input-group select {
            flex-grow: 1; /* Allow elements to grow and take up equal space */
            padding: 10px;
            background-color: transparent; /* Transparent background */
            border: 1px solid #fff; /* White border */
            color: #fff; /* Text color white */
            margin-right: 10px; /* Add margin between elements */
        }

        /* Label styles */
        .input-group label {
            color: #fff; /* White text color */
            margin-right: 10px; /* Add margin to the right */
        }

        /* Placeholder styles */
        .input-group ::placeholder {
            color: #fff; /* White placeholder text */
        }

        #gender option,#marital_status option, #relationship_status option{
    color: black; /* Set text color of options to black */
}

        #marital_status 
        {width: 185px;}



        /* Checkbox group styles */
        .checkbox-group {
            text-align: left;
            margin-bottom: 20px;
        }

        /* Checkbox label styles */
        .checkbox-group label {
            color: #fff; /* White text color */
            margin-right: 10px; /* Add margin to the right */
        }

        /* Checkbox input styles */
        .checkbox-group input[type="checkbox"] {
            display: inline-block;
            margin-right: 5px; /* Add margin to the right */
        }

        /* Button styles */
        .btn {
            width: 50%;
            padding: 10px;
            border: 1px solid #fff;
            margin: 0 auto;
            background-color: transparent;
            color: #fff;
            cursor: pointer;
        }

        /* Button hover effect */
        .btn:hover {
            background-color: #b24eff;
        }

        /* Responsive layout for smaller screens */
        @media only screen and (max-width: 600px) {
            .input-group input,
            .input-group select {
                width: 100%; /* Set width to 100% for smaller screens */
                margin-right: 0; /* Remove margin-right for smaller screens */
                margin-bottom: 10px; /* Reduce margin-bottom for smaller screens */
            }
        }
    </style>
</head>
<body>
<!-- Signup form container -->
<div class="signup-container">
    <!-- Signup form header -->
    <h2>Patient Registration</h2>
    <!-- Signup form -->
    <form action="/patientregister" method="POST">
        <!-- Input group for name and email -->
        <div class="input-group">
            <input type="text" name="name" placeholder="First Name" required>
            <input type="text" name="last_name" placeholder="Last Name" required>
        </div>
        <div class="input-group">
            <input type="number" name="Phone" placeholder="Phone" required>
            <input type="email" name="email" placeholder="Email" required>
        </div>
        <!-- Input group for password and confirm password -->
        <div class="input-group">
            <input type="password" name="password" placeholder="Password" required>
            <input type="password" name="confirm_password" placeholder="Confirm Password" required>
        </div>
        <!-- Input group for age and gender -->
        <div class="input-group">
            <input type="number" id="age" name="age" placeholder="Age" required>
            <select id="gender" name="gender" required>
                <option value="">Select Gender</option>
                <option value="male">Male</option>
                <option value="female">Female</option>
                <option value="other">Other</option>
            </select>
        </div>
        <!-- Input group for marital status and relationship status -->
        <div class="input-group">
            <select id="marital_status" name="marital_status" required>
                <option value="">Select Marital Status</option>
                <option value="single">Single</option>
                <option value="married">Married</option>
                <option value="divorced">Divorced</option>
                <option value="widowed">Widowed</option>
            </select>
            <select id="relationship_status" name="relationship_status" required>
                <option value="">Select Relationship Status</option>
                <option value="single">Single</option>
                <option value="in_relationship">In a relationship</option>
                <option value="complicated">It's complicated</option>
            </select>
        </div>
        <!-- Checkbox group for therapy types -->
        <div class="checkbox-group">
            <p style="padding-top: 15px; margin-bottom: 2px;">What kind of therapy are you seeking?</p>
            <!-- First row of checkboxes -->
            <div>
                <input type="checkbox" id="cb1" name="therapy_type" value="child">
                <label for="cb1">Child Therapy</label>
                <input type="checkbox" id="cb2" name="therapy_type" value="couple">
                <label for="cb2">Couple Therapy</label>
                <input type="checkbox" id="cb5" name="therapy_type" value="fear_phobia">
                <label for="cb5">Fear and Phobia Therapy</label>
            </div>
            <!-- Second row of checkboxes -->
            <div>
                <input type="checkbox" id="cb3" name="therapy_type" value="stress">
                <label for="cb3">Stress Therapy</label>
                <input type="checkbox" id="cb4" name="therapy_type" value="depression">
                <label for="cb4">Depression Therapy</label>
               
    <input type="checkbox" id="cb6" name="therapy_type" value="personal_growth">
    <label for="cb6">Personal Growth Therapy</label>
            </div>
        </div>

      <!-- Already have an account? Sign in -->
      <p>Already have an account? <a href="/patientlogin">Sign in</a></p>    
        <!-- Submit button -->
        <button type="submit" class="btn">Register</button>
    </form>
</div>
</body>
</html>
